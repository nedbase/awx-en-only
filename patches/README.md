# Drop .patch files here to have them applied to the upstream AWX source
# before the Docker image is built.
#
# Generate a patch with:
#   git format-patch HEAD~1          # last commit
#   git diff HEAD~1 HEAD > fix.patch # or manually
